using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Patterns_OO.Estruturais.Bridge_Pattern
{
    /* Bridge é um padrão de projeto de software utilizado quando é desejável que uma interface (abstração) possa variar 
     * independentemente das suas implementações. Através do padrão Bridge, a hierarquia que define os tipos de janelas é separada da hierarquia que 
     * contém a implementação. Desta forma todas as operações de Janela são abstratas e suas implementações são escondidas dos clientes. */
    public class Bridge
    {
    }
}